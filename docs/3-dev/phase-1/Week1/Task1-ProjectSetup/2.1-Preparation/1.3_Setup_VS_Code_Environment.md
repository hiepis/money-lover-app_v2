# 1.3 Prepare Development Environment (VS Code with React Extensions)

## Overview
Set up VS Code IDE with essential React and JavaScript development extensions to create an optimal development environment for the Personal Expense Tracker project.

## Prerequisites
- Operating system: Windows, macOS, or Linux
- Internet connection for downloads and extensions
- Node.js and npm installed (from previous steps)

## Steps

### 1. Install VS Code
```bash
# Download from official website
# https://code.visualstudio.com/

# Or install via package manager
# Windows (using winget)
winget install Microsoft.VisualStudioCode

# macOS (using Homebrew)
brew install --cask visual-studio-code

# Ubuntu/Debian
sudo apt update && sudo apt install code
```

### 2. Essential React Development Extensions

#### Core Extensions
```bash
# Install via command line (optional)
code --install-extension ms-vscode.vscode-typescript-next
code --install-extension bradlc.vscode-tailwindcss
code --install-extension esbenp.prettier-vscode
code --install-extension dbaeumer.vscode-eslint
```

#### Must-Have Extensions List
1. **ES7+ React/Redux/React-Native snippets** - `dsznajder.es7-react-js-snippets`
2. **Prettier - Code formatter** - `esbenp.prettier-vscode`
3. **ESLint** - `dbaeumer.vscode-eslint`
4. **Auto Rename Tag** - `formulahendry.auto-rename-tag`
5. **Bracket Pair Colorizer 2** - `coenraads.bracket-pair-colorizer-2`
6. **Tailwind CSS IntelliSense** - `bradlc.vscode-tailwindcss`
7. **TypeScript Importer** - `pmneo.tsimporter`
8. **Path Intellisense** - `christian-kohler.path-intellisense`
9. **GitLens** - `eamodio.gitlens`
10. **Thunder Client** - `rangav.vscode-thunder-client` (for API testing)

### 3. VS Code Configuration

#### User Settings (settings.json)
```json
{
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.formatOnSave": true,
  "editor.formatOnPaste": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "emmet.includeLanguages": {
    "javascript": "javascriptreact"
  },
  "emmet.triggerExpansionOnTab": true,
  "editor.tabSize": 2,
  "editor.insertSpaces": true,
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000,
  "editor.minimap.enabled": true,
  "editor.wordWrap": "on",
  "terminal.integrated.defaultProfile.windows": "Command Prompt"
}
```

#### Workspace Settings (.vscode/settings.json)
```json
{
  "typescript.preferences.importModuleSpecifier": "relative",
  "javascript.preferences.importModuleSpecifier": "relative",
  "emmet.includeLanguages": {
    "javascript": "javascriptreact"
  }
}
```

### 4. Debugging Configuration

#### Launch Configuration (.vscode/launch.json)
```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Chrome",
      "request": "launch",
      "type": "chrome",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}/src",
      "sourceMaps": true
    },
    {
      "name": "Attach to Chrome",
      "port": 9222,
      "request": "attach",
      "type": "chrome",
      "webRoot": "${workspaceFolder}/src"
    }
  ]
}
```

#### Tasks Configuration (.vscode/tasks.json)
```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "npm",
      "script": "dev",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "label": "npm: dev",
      "detail": "vite"
    },
    {
      "type": "npm",
      "script": "build",
      "group": "build",
      "label": "npm: build"
    }
  ]
}
```

### 5. Code Snippets Setup

#### Custom React Snippets (.vscode/snippets.json)
```json
{
  "Functional Component": {
    "prefix": "rfc",
    "body": [
      "import React from 'react';",
      "",
      "const ${1:ComponentName} = () => {",
      "  return (",
      "    <div>",
      "      $0",
      "    </div>",
      "  );",
      "};",
      "",
      "export default ${1:ComponentName};"
    ],
    "description": "Create a React functional component"
  }
}
```

### 6. Testing the Environment

#### Create Test React Component
```javascript
// test-component.jsx
import React, { useState } from 'react';

const TestComponent = () => {
  const [count, setCount] = useState(0);

  return (
    <div className="p-4 bg-blue-100">
      <h1 className="text-2xl font-bold">Test Component</h1>
      <p>Count: {count}</p>
      <button 
        onClick={() => setCount(count + 1)}
        className="px-4 py-2 bg-blue-500 text-white rounded"
      >
        Increment
      </button>
    </div>
  );
};

export default TestComponent;
```

#### Verification Checklist
- [ ] React snippets work (try typing `rfc` + Tab)
- [ ] Auto-completion for React hooks
- [ ] Prettier formats on save
- [ ] ESLint shows warnings/errors
- [ ] Tailwind CSS classes have IntelliSense
- [ ] Import paths auto-complete
- [ ] Debugging breakpoints work

## Recommended Themes and Icons

### Popular Themes
- **One Dark Pro** - `zhuangtongfa.material-theme`
- **Dracula Official** - `dracula-theme.theme-dracula`
- **Material Theme** - `equinusocio.vsc-material-theme`

### Icon Themes
- **Material Icon Theme** - `pkief.material-icon-theme`
- **VSCode Icons** - `vscode-icons-team.vscode-icons`

## Keyboard Shortcuts

### Essential Shortcuts
- `Ctrl+Shift+P` - Command palette
- `Ctrl+P` - Quick file open
- `Ctrl+Shift+E` - Explorer
- `Ctrl+`` ` - Toggle terminal
- `Ctrl+Shift+F` - Global search
- `F5` - Start debugging
- `Ctrl+Shift+I` - Developer tools

### React-Specific Shortcuts
- `Ctrl+Space` - Trigger IntelliSense
- `F12` - Go to definition
- `Shift+F12` - Find all references
- `Ctrl+.` - Quick fix

## Performance Optimization

### Settings for Better Performance
```json
{
  "files.watcherExclude": {
    "**/node_modules/**": true,
    "**/.git/objects/**": true,
    "**/.git/subtree-cache/**": true,
    "**/build/**": true,
    "**/dist/**": true
  },
  "search.exclude": {
    "**/node_modules": true,
    "**/build": true,
    "**/dist": true
  }
}
```

## Success Criteria
- [ ] VS Code installed and running
- [ ] All essential extensions installed and working
- [ ] Configuration files created and optimized
- [ ] React development workflow tested
- [ ] Debugging configuration verified
- [ ] Code formatting and linting working
- [ ] IntelliSense and auto-completion functional

## Troubleshooting

### Common Issues
1. **Extensions not working**: Reload VS Code window
2. **Prettier not formatting**: Check default formatter setting
3. **ESLint errors**: Ensure ESLint is installed in project
4. **Debugging not working**: Check port configuration

### Performance Issues
- Disable unused extensions
- Exclude large directories from file watcher
- Close unused editor tabs
- Use workspace-specific settings

## Related Issues
- **Parent Issue:** #1 Task 1: Project Setup & Initial Configuration
- **GitHub Issue:** #8
- **Previous Step:** #7 Update npm Version
- **Next Step:** #9 Review Project Requirements

## References
- [VS Code Official Documentation](https://code.visualstudio.com/docs)
- [React Development in VS Code](https://code.visualstudio.com/docs/nodejs/reactjs-tutorial)
- Project requirements: `docs/1-pdc.md`
- Implementation plan: `docs/2-plan.md`